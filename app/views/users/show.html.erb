<h1><%=@user.name%> </h1><br> <br>
<div>Neighborhood: <%=  link_to @user.neighborhood.name, neighborhood_path(@user.neighborhood_id)%></div> <br>
<div> Favorite Music Genre: <%=@user.fav_genre%></div> <br>

<br>

<div>Reviews Made: </div>

    <% if @user.reviews == []%>
        <div>No Reviews Made</div>
    <%else%>
        <ul>
            <% @user.reviews.each do |r| %>
                <li> Bar: <%=link_to r.dive_bar.name, dive_bar_path(r.dive_bar) %> <br> 
                Review: <%= r.review %> </li>
            <%end%>
        </ul>
    <%end%>
<br>

<%= link_to "Edit", edit_user_path(@user)%>
<%= link_to "Delete User", login_path, "method" => :delete %>